resource_groups = {
  acr_rg_re1 = {
    name = "acr-re1"           
    region = "region1"    
    tags = { 
      purpose = "{{zone_code}} acr resource group" 
      project-code = "{{project_code}}" 
      env = "{{caf_environment}}" 
      zone = "{{zone_code}}"
      tier = "{{tier_code}}"          
    }      
  }
}