# Create resource group
resource "azurerm_resource_group" "gcci_resource_groups" {
  for_each = var.gcci_resource_groups
  name     = "${each.value["name"]}${var.random_string}"
  location = var.location
}