managed_identities = {
  shared_kv_mi = {
    name               = "{{env_code}}{{dash_1}}{{zone_code}}{{dash_2}}{{tier_code}}{{dash_last}}shared-kv-mi"
    resource_group_key = "kv_rg_re1"

    tags = { 
      purpose = "{{tag_zone_code}} keyvault managed identity" 
      # project_code = "{{project_code}}" 
      env = "{{caf_environment}}" 
      zone = "{{tag_zone_code}}"
      tier = "{{tag_tier_code}}"          
    } 

  }
}
