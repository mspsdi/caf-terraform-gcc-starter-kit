resource_groups = {
  cosmodb_rg_re1 = {
    name = "cosmodb-re1"           
    region = "region1"    
    tags = { 
      purpose = "{{zone_code}} mssql resource group" 
      project-code = "{{project_code}}" 
      env = "{{caf_environment}}" 
      zone = "{{zone_code}}"
      tier = "{{tier_code}}"          
    }     
  }
}