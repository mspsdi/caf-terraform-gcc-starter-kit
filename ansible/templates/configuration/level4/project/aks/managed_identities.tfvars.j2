managed_identities = {
  aks_usermsi = {
    name               = "{{env_code}}{{dash_1}}{{zone_code}}{{dash_2}}{{tier_code}}{{dash_last}}aks-usermsi" 
    resource_group_key = "aks_re1"

    tags = { 
      purpose = "{{tag_zone_code}} aks managed identity" 
      # project_code = "{{project_code}}" 
      env = "{{caf_environment}}" 
      zone = "{{tag_zone_code}}"
      tier = "{{tag_tier_code}}"          
    }  
  }
}