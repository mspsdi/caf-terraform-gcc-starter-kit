resource_groups = {
  vm_region1 = {
    name = "{{env_code}}{{dash_1}}{{zone_code}}{{dash_2}}{{tier_code}}{{dash_last}}app-windows-ingress-internet"
    region = "region1"      
    tags = { 
      purpose = "{{tag_zone_code}} vm tooling server resource group" 
      project_code = "{{project_code}}" 
      env = "{{caf_environment}}" 
      zone = "{{tag_zone_code}}"
      tier = "{{tag_tier_code}}"          
    }    
  }
}

