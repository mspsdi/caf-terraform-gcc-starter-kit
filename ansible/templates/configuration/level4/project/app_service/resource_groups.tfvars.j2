resource_groups = {
  webapp_rg_re1 = {
    name = "{{env_code}}{{dash_1}}{{zone_code}}{{dash_2}}{{tier_code}}{{dash_last}}webapp-re1"           
    region = "region1"    
    tags = { 
      purpose = "{{tag_zone_code}} web app resource group" 
      # project_code = "{{project_code}}" 
      env = "{{caf_environment}}" 
      zone = "{{tag_zone_code}}"
      tier = "{{tag_tier_code}}"          
    }     
  }
}