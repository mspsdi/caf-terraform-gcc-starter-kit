managed_identities = {
  apim_msi = {
    name               = "{{env_code}}{{dash_1}}{{zone_code}}{{dash_2}}{{tier_code}}{{dash_last}}apim" 
    resource_group_key = "apim_rg_re1"
    tags = { 
      purpose = "{{tag_zone_code}} apim managed identity" 
      # project_code = "{{project_code}}" 
      env = "{{caf_environment}}" 
      zone = "{{tag_zone_code}}"
      tier = "{{tag_tier_code}}"          
    }  
  }
}