resource_groups = {
  bastion_rg_re1 = {
    name = "bastion-host"           
    region = "region1"    
    tags = { 
      purpose = "{{zone_code}} bastion resource group" 
      project-code = "{{project_code}}" 
      env = "{{caf_environment}}" 
      zone = "{{zone_code}}"
      tier = "{{tier_code}}"          
    }    
  }
}