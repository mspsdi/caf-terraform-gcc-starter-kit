resource_groups = {
  apim_rg_re1 = {
    name = "apim-rg-re1"           
    region = "region1"    
    tags = { 
      purpose = "{{zone_code}} apim resource group" 
      project-code = "{{project_code}}" 
      env = "{{caf_environment}}" 
      zone = "{{zone_code}}"
      tier = "{{tier_code}}"          
    }  
  }
}

