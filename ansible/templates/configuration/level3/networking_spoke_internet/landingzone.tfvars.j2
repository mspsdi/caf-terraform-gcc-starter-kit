landingzone = {
  backend_type        = "azurerm"
  global_settings_key = "shared_services"
  level               = "level3"
  key                 = "networking_spoke_internet" 
  tfstates = {
    launchpad = {
      level   = "lower"
      tfstate = "caf_launchpad.tfstate" # set to launchpad tfstate name
    } 
    shared_services = {
      level   = "current"
      tfstate = "shared_services.tfstate" # set to referenced landingzone tfstate name      
    }    
    networking_spoke_management = { # vnet link for private dns
      level   = "current"
      tfstate = "networking_spoke_management.tfstate" # set to referenced landingzone tfstate name      
    }            
  }
}
