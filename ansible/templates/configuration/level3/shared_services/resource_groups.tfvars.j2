{% if gcc_platform_import | trim != '2' %}
#------------------------------------------------------
# this page is leave blank intentionally
#------------------------------------------------------
{% else %}
resource_groups = {
  ops_rg_re1 = {
    name   = "ops-re1"
    region = "region1"
    tags = { 
      purpose = "shared services resource groups" 
      project_code = "{{project_code}}" 
      env = "{{caf_environment}}" 
      zone = "{{tag_zone_code}}"
      tier = "{{tag_tier_code}}"        
    }   
  }
  # Default to var.global_settings.default_region. You can overwrite it by setting the attribute region = "region2"
  networking_re1 = {
    name   = "{{resource_group_name}}"
    tags = { 
      purpose = "{{resource_group_name}} resource group" 
      project_code = "{{project_code}}" 
      env = "{{caf_environment}}" 
      zone = "{{tag_zone_code}}"
      tier = "{{tag_tier_code}}"           
    }  
  }
  law_re1 = {
    name   = "gcci-agency-law"
    tags = { 
      purpose = "gcci-agency-law resource group" 
      project_code = "{{project_code}}" 
      env = "{{caf_environment}}" 
      zone = "{{tag_zone_code}}"
      tier = "{{tag_tier_code}}"        
    }  
  }  
}
{% endif %}