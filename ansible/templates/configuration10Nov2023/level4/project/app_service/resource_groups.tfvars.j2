resource_groups = {
  webapp_rg_re1 = {
    name = "webapp-re1"           
    region = "region1"    
    tags = { 
      purpose = "{{zone_code}} web app resource group" 
      project-code = "{{project_code}}" 
      env = "{{caf_environment}}" 
      zone = "{{zone_code}}"
      tier = "{{tier_code}}"          
    }     
  }
}