
keyvaults = {
  level0 = {
    name                = "level0"
    resource_group_key  = "level0"
    sku_name            = "standard"
    # soft_delete_enabled = true # enabled this line when production
    soft_delete_enabled = false 
    tags = {
      tfstate     = "level0"
      environment = "sandpit"
    }

    creation_policies = {
      logged_in_user = {
        # if the key is set to "logged_in_user" add the user running terraform in the keyvault policy
        # More examples in /examples/keyvault
        secret_permissions = ["Set", "Get", "List", "Delete", "Purge", "Recover"]
      }
    }

  }

  level3 = {
    name                = "level3"
    resource_group_key  = "level3"
    sku_name            = "standard"
    soft_delete_enabled = false
    tags = {
      tfstate     = "level3"
      environment = "sandpit"
    }

    creation_policies = {
      logged_in_user = {
        # if the key is set to "logged_in_user" add the user running terraform in the keyvault policy
        # More examples in /examples/keyvault
        secret_permissions = ["Set", "Get", "List", "Delete", "Purge", "Recover"]
      }
    }
  }

  level4 = {
    name                = "level4"
    resource_group_key  = "level4"
    sku_name            = "standard"
    soft_delete_enabled = false
    tags = {
      tfstate     = "level4"
      environment = "sandpit"
    }

    creation_policies = {
      logged_in_user = {
        # if the key is set to "logged_in_user" add the user running terraform in the keyvault policy
        # More examples in /examples/keyvault
        secret_permissions = ["Set", "Get", "List", "Delete", "Purge", "Recover"]
      }
    }

  }




}
