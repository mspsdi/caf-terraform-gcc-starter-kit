resource_groups = {
  vm_region1 = {
    name = "vm-windows-tooling"
    region = "region1"      
    tags = { 
      purpose = "{{zone_code}} vm tooling server resource group" 
      project-code = "{{project_code}}" 
      env = "{{caf_environment}}" 
      zone = "{{zone_code}}"
      tier = "{{tier_code}}"          
    }    
  }
}

